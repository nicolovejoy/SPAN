.env
__pycache__/
*.pyc
.venv/
pi/collector.log
pi/bath_detector.log
pi/cloudflared/

# Playwright MCP browser-automation scratch (console logs + page snapshots)
.playwright-mcp/
.vercel

# git worktrees created by EnterWorktree — never commit these
.claude/worktrees/

# Ad-hoc Influx query dumps (query_usage.sh) — never committed
/tmp/
